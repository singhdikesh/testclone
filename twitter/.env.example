DATABASE_URL="postgresql://postgres:password@localhost:5432/clone"
JWT_SECRET="replace-with-a-long-random-secret"

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
EMAIL_FROM=your-email@gmail.com
FRONTEND_URL=http://localhost:5173

GOOGLE_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com# Copy this file to `.env` and replace the placeholder with your real connection string.
# Connection string for PostgreSQL.
# Requires PostgreSQL >= 15.

DATABASE_URL="postgresql://user:password@localhost:5432/mydb"
